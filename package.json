{
  "name": "meridian-schemas-workspace",
  "version": "0.1.0",
  "private": true,
  "description": "Workspace root for meridian-schemas. The published npm packages have their own manifests: proto/proto-ts.package.json (@savvifi/meridian-proto-ts) and schemas.package.json (@savvifi/meridian-schemas). This manifest only carries the build/codegen toolchain (protobuf-es + tsc).",
  "type": "module",
  "dependencies": {
    "@bufbuild/protobuf": "2.12.1"
  },
  "devDependencies": {
    "@bufbuild/protoc-gen-es": "2.12.1",
    "typescript": "5.6.2"
  },
  "pnpm": {
    "onlyBuiltDependencies": ["esbuild"]
  }
}
